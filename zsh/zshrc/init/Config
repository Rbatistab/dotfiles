#!/bin/bash

# Autosuggestions
source ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh

# Keybindings
bindkey -v 
bindkey -r '^L' # unbind ctrl-l for clear screen
bindkey '\el' clear-screen # re-bind it to alt-g

